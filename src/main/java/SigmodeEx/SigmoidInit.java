package SigmodeEx;

public class SigmoidInit {
    public static void main(String[] args) {
        double x = 2;
        double result;
        result = 1 / (1 + Math.pow(Math.E,(x * (-1))));
    }
}
